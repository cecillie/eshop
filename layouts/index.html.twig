{% extends 'page.html.twig' %}

{% block content %}
      <div class="hero">
        {{~ page.content ~}}
      </div>
      <div class="products">
        {%- for product in site.pages.all|filterBy('section', 'products')|sortByWeight ~%}
          {%~ include 'components/product.html.twig' with {'product': product} only ~%}
        {%- endfor ~%}
      </div>
{% endblock %}
