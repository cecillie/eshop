{#- variables: product, image -#}
{%- set default = asset('images/default.png') ~%}
          <div class="carousel" data-flickity='{ "imagesLoaded": true, "percentPosition": false, "prevNextButtons": false, "fullscreen": true, "lazyLoad": true }'>
            {#- main image ~#}
            <img src="{{ default|url }}" data-flickity-lazyload="{{ image|default(default)|url }}" alt="Image de {{ product.name }}" loading="lazy" />
            {#- gallery images ~#}
            {%- if product.gallery is defined ~%}
              {%- for img in product.gallery ~%}
            <img src="{{ default|url }}" data-flickity-lazyload="{{ asset(img)|default(default)|url }}" alt="Image de {{ product.name }}" />
              {%- endfor ~%}
            {%- endif ~%}
          </div>
